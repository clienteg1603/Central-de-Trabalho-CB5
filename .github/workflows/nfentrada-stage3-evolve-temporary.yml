name: NF Entrada stage 3 evolve temporary

on:
  push:
    paths:
      - 'src/current/NFENTRADA_STAGE3_READY'

permissions:
  contents: write

jobs:
  evolve:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Aplicar evolução da etapa 3
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          python ".\src\current\transforms\0214_nfentrada_stage3_core_ui.py"
          python ".\src\current\transforms\0214_nfentrada_stage3_meta.py"

      - name: Validar histórico, backups, restauração e sintaxe no Windows PowerShell
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          & ".\src\ci\test_nfentrada_import.ps1" -CorePath ".\src\generated\Modulos\Controle-NF-Entrada\NFEntrada.Core.ps1"
          & ".\src\ci\test_nfentrada_stage1.ps1" -CorePath ".\src\generated\Modulos\Controle-NF-Entrada\NFEntrada.Core.ps1"
          & ".\src\ci\test_nfentrada_stage2.ps1" -CorePath ".\src\generated\Modulos\Controle-NF-Entrada\NFEntrada.Core.ps1"
          & ".\src\ci\test_nfentrada_stage3.ps1" -CorePath ".\src\generated\Modulos\Controle-NF-Entrada\NFEntrada.Core.ps1"
          & ".\src\ci\parse_powershell.ps1" -Root ".\src\generated"
          python ".\src\ci\regression_contracts.py" ".\src\generated"

      - name: Publicar fontes validados
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          Remove-Item -LiteralPath "src/current/NFENTRADA_STAGE3_READY" -Force
          Remove-Item -LiteralPath "src/current/transforms/0214_nfentrada_stage3_core_ui.py" -Force
          Remove-Item -LiteralPath "src/current/transforms/0214_nfentrada_stage3_meta.py" -Force
          git config user.name "Central de Trabalho Builder"
          git config user.email "actions@users.noreply.github.com"
          git add 'src/generated/Modulos/Controle-NF-Entrada/NFEntrada.Core.ps1'
          git add 'src/generated/Modulos/Controle-NF-Entrada/Controle NF Entrada.ps1'
          git add 'src/generated/Central de Trabalho.ps1'
          git add 'src/ci/regression_contracts.py'
          git add 'src/ci/test_nfentrada_stage3.ps1'
          git add 'src/current/version.json'
          git add -u 'src/current/NFENTRADA_STAGE3_READY'
          git add -u 'src/current/transforms/0214_nfentrada_stage3_core_ui.py'
          git add -u 'src/current/transforms/0214_nfentrada_stage3_meta.py'
          git commit -m "Evolui Controle NF Entrada - etapa 3 histórico e segurança"
          git push
